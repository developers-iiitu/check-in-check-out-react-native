export function mobileValidator(num) {
//   const re = /\S+@\S+\.\S+/
  if (!num) return "Mobile num can't be empty."
//   if(num>9999999999 || num<1000000000) return "Enter a valid Mobile Number"
  return ''
}